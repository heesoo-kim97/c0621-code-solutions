/* exported isUpperCased */
function isUpperCased(word) {
  if (word === word.toUpperCase()) {
    return true;
  } else {
    return false;
  }
}

// Using a if statement we can compare to see if the string is strictly equal to uppercase version of the string to return true
