/* exported getFirstChar */
function getFirstChar(string) {
  return string[0];
}

// string value is entered in the parameter and string value at index 0 (the first character) is returned from the function
