/* exported getLastChar */
function getLastChar(string) {
  return string[string.length - 1];
}

// Last Chatacter of string value needs to be returned. string.length -1 gives us the last character of a string.
